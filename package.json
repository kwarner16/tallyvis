{
  "name": "tallyvis",
  "private": true,
  "version": "0.1.0",
  "description": "Tallyvis — AI visual estimating and quoting platform for physical service businesses.",
  "packageManager": "pnpm@12.4.2",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "prettier": "^3.3.3",
    "turbo": "^2.3.0",
    "typescript": "^5.6.3",
    "typescript-eslint": "^8.15.0"
  }
}
